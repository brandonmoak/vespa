import vespa
import constraints

# The executive type that will be loaded
executive = vespa.config.agents.swarm_exec

# entities in the swarm
entities = ['first']

# predefined static constraints
constraints = constraints.defined_constraints
